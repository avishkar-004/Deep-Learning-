# Contributing

Each assignment notebook is self-contained and can be run in Google Colab.

## Running Locally

```bash
pip install -r requirements.txt
jupyter notebook
```

## Project Structure

- `Assignments/` - Jupyter notebooks for each assignment
- `scripts/` - Standalone utility scripts
- `utils/` - Shared helper modules
- `results/` - Experiment notes and summaries
